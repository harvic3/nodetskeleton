export class NumberUtil {
  static ZERO = 0;
  static ONE = 1;
  static DELETE_ONE = 1;
  static TIME_OUT_ZERO = 0;
  static INDEX_ONE = 1;

  static isEvenNumber(value: number): boolean {
    return value % 2 === 0;
  }
}
